package main.java.co.hsbc.Enums;

public class EmployeeEnums {
    public enum Role {
        user,
        admin,
        doctor
    }
    public enum Department {
        general,
        emergency
    }
    public enum Shift {
        day,
        night
    }
}
